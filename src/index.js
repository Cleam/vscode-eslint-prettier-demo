console.log('aaa');

/**
 *求和
 *
 * @param {Number} a 参数1（必须）
 * @param {Number} b 参数2（必须）
 * @returns 求和结果
 */
function sum(a, b) {
  return a + b;
}

console.log(sum(3, 4));

function square(x) {
  return x ** x;
}

console.log(square(5));
